export interface SavedPagesINT {
    key?: string; //means key is optional, when Addpage no need input, firebase will provide
    // id: number;
    landmarkName: String;
    landmarkDescr: String;
    landmarkAdd: String;
    landmarkTime: String;
    landmarkTag: String;   
    landmarkImg: String; 
}